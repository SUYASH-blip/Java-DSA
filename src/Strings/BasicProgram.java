package Strings;

public class BasicProgram {
    //next() method only takes a word from a string
    //while nextLine() method reads whole string in double quotes
    //Strings contains indexing because internally it is char array
    //But in java we cannot directly access elements using the [] brackets
    //instead we use methods like charAt(index)
    //And we access length using String_name.length().
}
